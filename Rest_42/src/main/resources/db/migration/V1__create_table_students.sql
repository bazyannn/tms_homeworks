create table students(
                         id uuid primary key,
                         name varchar,
                         surname varchar,
                         sex varchar,
                         year_of_birthday int,
                         number_of_class int,
                         address varchar
);